# Vite environment variables
# Copy this file to .env.local and adjust values as needed

# Set to true to enable real API calls (otherwise app uses localStorage mocks)
VITE_USE_API=false

# Base URL of your backend (no trailing slash)
# Example: http://localhost:3000
VITE_API_BASE_URL=

# Optional: override resource paths if they differ from defaults
# Defaults used by the frontend: /packages, /bookings, /users/me
# VITE_API_PATH_PACKAGES=/api/v1/packages
# VITE_API_PATH_BOOKINGS=/api/v1/bookings
# VITE_API_PATH_ME=/api/v1/users/me
